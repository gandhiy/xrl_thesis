{

  run_name: 'PPO1_tune',
  model: 'PPO1',
  environment: 'CartPole-v0',
  logging: True, # tensorboard logging
  save_model: False,
  
  environment_parameters: {},

  learning_parameters: {
    timesteps: 1000
  },

  # model parameters may need to be changed based on model type
  model_parameters: {
    verbose: 1
  },

  testing_parameters: {
    render: False,
    iterations: 1000
  }
  
}